program Test {
  int i,j,k;
  bool valor;
  void SumUp() {
    int sum;
    sum = 0;
    while (i > 0) { sum = sum + i; i = i - 1; } 
    write sum;
  }
  void Main() {
    read i;
    while (i > 0) {
      SumUp();
      read i;
    }
  }
}
